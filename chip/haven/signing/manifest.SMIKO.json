{
    // Ignore all fuses and INFO values.
    "fuses": {},
    "info": {},

    // Don't set the board ID in any images.
    "board_id": 0,
    "board_id_mask": 0,
    "board_id_flags": 0,

    "tag": "Smiko CFW v1.8.131, H1-Cr50 GSC, by HavenOverflow, 2025 ",

    // Use a high version value so the image always writes and runs first.
    "epoch": 1,
    "major": 8,
    "minor": 131,

    "keyid": 924249707, // BootROM developer key to make RSA bruteforcing easier.
    "p4cl": 177, // P4CL XML number (177 == 0xb1)

    // Something larger than 3 to make _purgatory never succeed.
    // This will ignore regtable violations and allow fault injection in boot_sys.
    "expect_response": 4,
    "err_response": 0, // Don't let any bits get ORd with ERR_RESPONSE.

    // Disable any extra BootROM actions before launching the image.
    "config1": 0,

    // Don't engage any alerts.
    "applysec": 0
}